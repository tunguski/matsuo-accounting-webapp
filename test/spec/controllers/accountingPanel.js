'use strict';

describe('AccountingPanelCtrl', function () {
  var controller;

  beforeEach(inject(function ($httpBackend, $rootScope, $controller) {
    controller = $controller('AccountingPanelCtrl', { $scope: scope });
  }));


  it('', function () {
  });
});
